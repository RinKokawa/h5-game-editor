/**
 * Shared: Constants.
 *
 * App-wide constants that have no business logic — grid color, default tile
 * size, etc. Anything that depends on Document or runtime state does NOT
 * live here.
 */
export {};
