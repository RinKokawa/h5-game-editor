/**
 * Panels: Inspector.
 *
 * Renders the properties of the current selection. Field renderers are
 * registered through the Extension Registry so future entity types can ship
 * custom fields.
 */
export {};
