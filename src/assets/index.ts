/**
 * Asset management.
 *
 * Loading, caching, and metadata for project assets (images, tilesets, audio).
 * Files are referenced by URL/path; assets themselves are not inlined into
 * the Document schema.
 */
export {};
