/**
 * Camera subsystem — public surface.
 *
 * The Camera owns the Pixi Container that world-space rendering targets
 * attach themselves to. Construction is the only public entry point;
 * everything else is renderer-internal.
 */

export { Camera } from './Camera';
