/**
 * Canvas: Input aggregation.
 *
 * Collects pointer / wheel / keyboard events at the Pixi host element and
 * routes them to the active Tool. Tools never attach DOM listeners directly.
 */
export {};
