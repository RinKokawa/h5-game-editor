/**
 * Core: Extension Registry.
 *
 * Allows future editor types (Dialogue, Animation, Quest, ...) and third-party
 * plugins to register tools, commands, panels, entity types, and serializers
 * without modifying the framework.
 */
export {};
